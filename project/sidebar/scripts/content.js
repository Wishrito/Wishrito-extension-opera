// content.js

// Fonction qui sera exécutée lors du chargement de la barre latérale
function initSidebar() {
    // Insérer le code pour initialiser la barre latérale ici
    console.log("Extension de la barre latérale initialisée.");
}

// Exécuter la fonction d'initialisation une fois la page chargée
document.addEventListener('DOMContentLoaded', function () {
    initSidebar();
});
